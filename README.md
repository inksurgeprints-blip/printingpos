# Inksurge Prints — POS

React + Vite + Tailwind point-of-sale app, with Firebase (Firestore) for cloud data sync.

## Local dev
```
npm install
cp .env.example .env.local   # then fill in your Firebase values
npm run dev
```

## Deploy
See the setup guide provided alongside this project for Firebase project creation and Vercel deployment steps.
